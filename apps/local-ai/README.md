# NNIT Local AI Worker V9

Standard-library local service on `127.0.0.1:8765`.

It provides health/capability APIs and deterministic WAV analysis for mix/master assistant jobs. Neural denoise, separation and pitch correction are deliberately reported as `needs-model` until an actual local model adapter is installed.
